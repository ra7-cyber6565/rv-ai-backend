name: Temporary dispatch PR113 zero-cost model probe

on:
  push:
    branches:
      - test/dispatch-pr113-zero-cost-probe
    paths:
      - .github/workflows/temp-dispatch-pr113-zero-cost-probe.yml

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch reviewed PR113 model probe
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          payload='{"ref":"integration/pr81-trading-on-pr111","inputs":{"live_company":"false","model_probe_only":"true","reviewed_commit":"43c411cad88b226be44892152c0fa624edd8b68e"}}'
          code=$(curl --silent --show-error --output /tmp/dispatch.out --write-out '%{http_code}' \
            --request POST \
            --header 'Accept: application/vnd.github+json' \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/${REPO}/actions/workflows/foundation-tests.yml/dispatches" \
            --data "$payload")
          if [ "$code" != "204" ]; then
            echo "workflow dispatch failed with HTTP $code" >&2
            exit 2
          fi
          echo "Exact reviewed PR113 model probe dispatched."
