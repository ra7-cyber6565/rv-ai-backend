Current-main sync is being reconciled on the integration branch without claiming test success. The merge keeps the reviewed integration/security/storage overrides and records current main as an explicit parent after content comparison.
