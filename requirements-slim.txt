# Infinity Research AI — SLIM install (chhote free host ke liye, jaise Render free)
#
#     pip install -r requirements-slim.txt
#
# Yeh `requirements.txt` ki JAGAH nahi leta. Poora (primary) host — jaise Railway
# — hamesha `requirements.txt` hi use karega. Yeh file sirf UN hosts ke liye hai
# jahan 512 MB RAM aur chhota build space hai.
#
# Kya nikaala gaya aur KYUN
# ------------------------
#   chromadb              local vector database + ONNX embedding runtime
#
# Primary requirements ab alag `sentence-transformers`/PyTorch stack install
# nahi karte. Primary PDF/notes vector search Chroma ke built-in local ONNX
# all-MiniLM-L6-v2 embedding function se chalta hai. Slim backup par `chromadb`
# khud hi hata hua hai, isliye user-uploaded PDF vector search wahan available
# nahi hota.
#
# Poora research pipeline — web search, relevance, facet gate, claim
# verification, hypothesis, audit — chromadb ke bina chalta rehta hai, kyunki
# `research_engine/vector_search.py` RAG ko lazy import karta hai aur unavailable
# hone par khaali context deta hai (koi crash nahi, koi jhootha jawab nahi).
#
# Iska seedha matlab, saaf-saaf: is host par tumhaari khud ki upload ki hui PDF
# se search BAND rahegi. Baaki sab chalega. Yeh feature primary host se nahi
# hataya gaya; sirf chhote backup host ki imaandaar seema hai.
#
# Jo packages dono files me hain unke version pins HUBAHU same rakhe gaye hain,
# taaki dono host ek hi code par ek hi tarah chalein.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.5
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==5.3.0
requests==2.32.3
google-generativeai==0.8.3
python-dotenv==1.0.1
PyMuPDF==1.25.2
Pillow==11.1.0
scipy==1.15.1
sympy==1.13.3
numpy==2.2.2
tiktoken==0.8.0
